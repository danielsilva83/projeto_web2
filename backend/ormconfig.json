{
   "type": "mysql",  
   "url": "mysql://root:30195dgs@localhost:3306/temrazao",
   "synchronize": true,
   "logging": false,
   "entities": [
      "dist/**/*.entity{.ts,.js}"
   ],
   "migrations": [
      "dist/**/migration/**/*{.ts,.js}"
   ],
   "subscribers": [
      "src/subscriber/**/*.ts}"
   ],
   "cli": {
      "entitiesDir": "src/**/*.entity{.ts,.js}",
      "migrations": "src/migration",
      "subscribers": "src/subscriber"
   }
}
